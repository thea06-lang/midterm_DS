import java.io.IOException;

/** P4: saves arrays and results to .txt files. */
public class OutputWriter {
    /** Write one number per line to path. Create the parent folder if missing. Use a BufferedWriter (1M lines). */
    public static void writeArray(String path, int[] data) throws IOException {
        // TODO (P4)
    }

    /** Write a block of text (the results table) to path. */
    public static void writeText(String path, String text) throws IOException {
        // TODO (P4)
    }
}
